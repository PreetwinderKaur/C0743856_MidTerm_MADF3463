package com.lambton;

public class Reverse {
        public static void main(String[] args)
        {
        string reverseMystring(String str)
        {
            int len = str.length();
            string temp = "";
            for (int i = len -1;
                 i >=0; i--)
            {
                temp +=str.charAt(i);
            }

        }
    }
}
